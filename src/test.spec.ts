import {test, expect, describe} from 'vitest'


describe('FILE SAMPLE TEST', () => {
  test('primeiro test', () => {
    const a = 1
    const b = 2
  
    const soma = a + b
  
    expect(soma).toBe(3)
  })
})
